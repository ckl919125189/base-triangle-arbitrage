{
  "name": "base-triangle-arbitrage",
  "version": "1.0.0",
  "description": "Triangle arbitrage smart contract for Base chain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy": "hardhat run scripts/deploy.js --network",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base",
    "node": "hardhat node",
    "dashboard": "npx serve src/dashboard -p 3000"
  },
  "keywords": [
    "arbitrage",
    "defi",
    "base",
    "ethereum",
    "flash-loan"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.5",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  },
  "dependencies": {}
}
